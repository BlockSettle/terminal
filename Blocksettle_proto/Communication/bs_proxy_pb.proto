syntax = "proto3";

package Blocksettle.Communication.ProxyPb;

message Message
{
    string email = 1;

    // Serialized Blocksettle.Communication.Otc.PbRequest (from proxy)
    // or Blocksettle.Communication.Otc.PbResponse (from server)
    bytes data = 2;
}
